<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ElorChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.ElorChat" parent="Base.Theme.ElorChat">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- New Colors for Dark Theme -->
        <item name="android:windowBackground">@color/colorMainBackgroundDark</item>
        <item name="android:colorBackground">@color/colorNavBarDetailsDark</item>
        <item name="android:textColor">@color/colorTextDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryAccentDark</item>
        <item name="android:textColorPrimary">@color/colorTextDark</item>

    </style>

    <style name="Theme.ElorChat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimaryDark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/circle_launcher</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.ElorChat</item>
    </style>

    <style name="CustomCheckBox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@drawable/custom_checkbox_selector</item>
    </style>
</resources>
